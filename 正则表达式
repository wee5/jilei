定义：一种字符串匹配模式，其子串符合时即为匹配
正则表达式匹配字符串和在idea中用正则表达式搜索的区别？
    idea中，将整个文件的每一行当做一个字符串，其包含换行空格在内的很多字符
    会匹配符合条件的子串，而不会匹配包含该子串的字符串。（若会匹配包含符合条件的子串的字符串，则只要有一个符合条件的子串，整个文件都会被匹配）
在正则表达式的示例中，通常会用//来包含一个正则表达式，其本身没有具体含义，实际使用时，不需要使用//包含整个正则表达式

普通字符：所有大写和小写字母、所有数字、所有标点符号和一些其他符号
非打印字符：\n(换行)，\r(回车),\s(空白字符)，\S(非空白字符)，\t(制表符)
特殊字符：不表达其本身含义，需要转义才能表达本身含义
    ^：开始标志
    $：结束标志
    ()：子表达式的开始和结束
    .：任意单个字符（除了换行符）
    \：转义下一个字符
    |：或

[]：其中的任意单个字符
常用转义字符：\w（字母，数字，下划线）

限定符：表示出现次数（n，m为非负整数）
    *：单个字符之后，或配合()，零次或多次
    +：单个字符之后，或配合()，一次或多次
    ?：单个字符之后，或配合()，零次或一次
    {n}：n次
    {n,}：至少n次
    {n,m}：n到m次（n<=m）
贪婪与非贪婪：*和+是贪婪限定符，会尽可能多的匹配字符串，在其后面加?实现非贪婪
定位符
    ^：开始标志
    $：结束标志
    \b：单词边界，字符串边缘是单词，但不匹配其边缘的单词
    \B：非单词边界匹配，字符串边缘是非单词，但不匹配其边缘的非单词

 ?=、?<=、?!、?<! 的使用区别
    exp1(?=exp2)：查找 exp2 前面的 exp1
    (?<=exp2)exp1：查找 exp2 后面的 exp1
    exp1(?!exp2)：查找后面不是 exp2 的 exp1
    (?<!exp2)exp1：查找前面不是 exp2 的 exp1

反向引用？

正则表达式中的[],(),{}的区别？
    []：任意单个字符
    ()：组
    {}：长度


    
    
